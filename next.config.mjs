/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // basePath: "/browser-expense-tracker",
  // assetPrefix: "/browser-expense-tracker/",
};

export default nextConfig;
